# Clerk
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_REPLACE_ME
CLERK_SECRET_KEY=sk_test_REPLACE_ME
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# Convex
NEXT_PUBLIC_CONVEX_URL=https://REPLACE_ME.convex.cloud

# Chat backend (Cloudflare Pages Function). Set as Cloudflare secrets, never in the repo:
#   npx wrangler pages secret put GROQ_API_KEY --project-name=monarch
# GROQ_MODEL is optional and defaults to llama-3.3-70b-versatile.
